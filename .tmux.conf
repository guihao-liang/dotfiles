# recap C-b to C-a
unbind C-b
set -g prefix C-a
bind-key C-a send-prefix

# 01/10/2017
# Mouse-mode has been rewritten.  There's now no longer options for:
#    - mouse-resize-pane
#    - mouse-select-pane
#    - mouse-select-window
#    - mode-mouse
# Instead there is just one option:  'mouse' which turns on mouse support

# 02/24/2017 Try to solve mouse mode legacy from 1.9 on my Debian. Failed.
# Solved it by using newer version tmux from site `jessies-backports`
# if-shell "if [[ `tmux -V | cut -d' ' -f2` -lt 2.1 ]]; then true; else false; fi" \
#    'setw -g mode-mouse on; setw -g mouse-resize-pane on; setw -g mouse-select-pane on; setw -g mouse-select-window on'
# if-shell "if [[ `tmux -V | cut -d' ' -f2` -ge 2.1 ]]; then true; else false; fi" \
#    'set -g mouse on; set -g mouse-utf8 on'
set -g mouse on

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
     | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind-key -n C-h if-shell "$is_vim" "send-keys C-h"  "select-pane -L"
bind-key -n C-j if-shell "$is_vim" "send-keys C-j"  "select-pane -D"
bind-key -n C-k if-shell "$is_vim" "send-keys C-k"  "select-pane -U"
bind-key -n C-l if-shell "$is_vim" "send-keys C-l"  "select-pane -R"
bind-key -n C-\ if-shell "$is_vim" "send-keys C-\\" "select-pane -l"

# set the default terminal 
set -g default-terminal "xterm-256color"

# enable <prefix> C-l to clear screen
bind C-l send-key 'C-l'

# https://github.com/tmux-plugins/tpm
# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'


# custom plugins
set -g @plugin 'christoomey/vim-tmux-navigator'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf) 
run '~/.tmux/plugins/tpm/tpm'
